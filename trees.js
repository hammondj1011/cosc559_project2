class Trees {
  constructor(x, y, r){
    this.x = width
    this.y = this.r
    this.r = 100
    
  }
  
  show(){

    image(cImg1, this.x, 150, 150, 110)
  }
  
  move(){
    this.x -= 0.4;
  }
  
}
